import 'package:championship/components/player.dart';
import 'package:flutter/material.dart';

class DrawScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Leider Unentschieden!"),
      ),
    );
  }
}
